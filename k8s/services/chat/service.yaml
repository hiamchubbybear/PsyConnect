apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: psyconnect
spec:
  selector:
    app: chat-service
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
  type: ClusterIP
