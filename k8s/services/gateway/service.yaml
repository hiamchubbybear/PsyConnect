apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: psyconnect
spec:
  selector:
    app: api-gateway
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
  type: ClusterIP
