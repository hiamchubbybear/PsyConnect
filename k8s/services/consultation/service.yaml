apiVersion: v1
kind: Service
metadata:
  name: consultation-service
  namespace: psyconnect
spec:
  selector:
    app: consultation-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP
