apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: psyconnect
spec:
  selector:
    app: profile-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
